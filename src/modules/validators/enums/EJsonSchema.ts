export enum EJsonSchemaTypes {
    OBJECT = 'object',
    STRING = 'string',
    NUMBER = 'number',
    ARRAY = 'array',
    BOOLEAN = 'boolean'
}

export enum EJsonSchemaFormat {
    EMAIL = 'email',
    DATE = 'date',
    DATE_TIME = 'date-time',
    TIME = 'time'
}